name: CI

on:
  push:
  pull_request:

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: portfolio_test
          POSTGRES_PASSWORD: portfolio_test
          POSTGRES_DB: portfolio_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U portfolio_test -d portfolio_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      TEST_DATABASE_URL: postgresql://portfolio_test:portfolio_test@localhost:5432/portfolio_test
      DATABASE_URL: postgresql://portfolio_test:portfolio_test@localhost:5432/portfolio_test
      DB_URL: postgresql://portfolio_test:portfolio_test@localhost:5432/portfolio_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run check:locale-config
      - run: npm run check:locale-coverage
      - run: npm run build
